name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    name: Validate ProjectX MVP
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: services/api/go.mod
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - name: Validate repository layout
        run: |
          test -f README.md
          test -f docs/MVP_SCOPE.md
          test -f docs/MICROSERVICES.md
          test -f contracts/openapi/README.md
          test -f contracts/openapi/api.yaml
          test -f contracts/openapi/ai.yaml
          test -f contracts/events/asyncapi.yaml

          test -f infra/db/migrations/001_init.sql
          test -f services/api/Dockerfile
          test -f services/api/cmd/server/main.go
          test -f services/ai/Dockerfile
          test -f services/ai/app/main.py
          test -x scripts/smoke.sh
          sh -n scripts/smoke.sh

      - name: Validate Docker Compose
        run: |
          cp .env.example .env
          # compose requires a non-empty BETTER_AUTH_SECRET; provide a throwaway for config validation
          echo "BETTER_AUTH_SECRET=ci-placeholder-not-a-real-secret" >> .env
          make compose-config
      - name: Test API
        run: make test-api
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Test AI integration flow
        run: make test-ai
      - name: Build web app
        run: make test-web
