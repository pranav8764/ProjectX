openapi: 3.1.0
info:
  title: "Legacy planning sketch: Asset Service"
  version: 0.1.0
  description: Legacy domain-service contract sketch for a possible future split. Asset APIs currently run in services/api.
x-contract-status: legacy-domain-sketch
paths:
  /health:
    get:
      responses:
        "200":
          description: Healthy
  /internal/assets:
    get:
      summary: List assets
      responses:
        "200":
          description: Asset list
  /internal/assets/{assetId}:
    get:
      summary: Get asset profile
      parameters:
        - name: assetId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Asset profile
