openapi: 3.1.0
info:
  title: "Legacy planning sketch: Identity Service"
  version: 0.1.0
  description: Legacy domain-service contract sketch for a possible future split. Identity APIs currently run in services/api.
x-contract-status: legacy-domain-sketch
paths:
  /health:
    get:
      responses:
        "200":
          description: Healthy
  /internal/permissions/check:
    post:
      summary: Check whether a user can perform an action
      responses:
        "200":
          description: Permission decision
