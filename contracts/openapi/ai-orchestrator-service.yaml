openapi: 3.1.0
info:
  title: "Legacy planning sketch: AI Orchestrator Service"
  version: 0.1.0
  description: Legacy domain-service contract sketch for a possible future split. AI orchestration currently runs in services/ai.
x-contract-status: legacy-domain-sketch
paths:
  /health:
    get:
      responses:
        "200":
          description: Healthy
  /internal/ai/generate:
    post:
      summary: Generate model output through configured provider
      responses:
        "200":
          description: Model response
  /internal/ai/embeddings:
    post:
      summary: Generate embeddings
      responses:
        "200":
          description: Embeddings
