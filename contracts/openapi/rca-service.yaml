openapi: 3.1.0
info:
  title: "Legacy planning sketch: RCA Service"
  version: 0.1.0
  description: Legacy domain-service contract sketch for a possible future split. RCA APIs currently run through services/api and services/ai.
x-contract-status: legacy-domain-sketch
paths:
  /health:
    get:
      responses:
        "200":
          description: Healthy
  /internal/rca/reports:
    post:
      summary: Generate RCA report
      responses:
        "202":
          description: RCA job accepted
    get:
      summary: List RCA reports
      responses:
        "200":
          description: RCA reports
