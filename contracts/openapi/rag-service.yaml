openapi: 3.1.0
info:
  title: "Legacy planning sketch: RAG Service"
  version: 0.1.0
  description: Legacy domain-service contract sketch for a possible future split. RAG APIs currently run through services/api and services/ai.
x-contract-status: legacy-domain-sketch
paths:
  /health:
    get:
      responses:
        "200":
          description: Healthy
  /internal/rag/query:
    post:
      summary: Run retrieval and generate a cited answer
      responses:
        "200":
          description: Cited answer
  /internal/rag/search:
    post:
      summary: Search relevant chunks without answer generation
      responses:
        "200":
          description: Retrieval results
