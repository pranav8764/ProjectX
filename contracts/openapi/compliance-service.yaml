openapi: 3.1.0
info:
  title: "Legacy planning sketch: Compliance Service"
  version: 0.1.0
  description: Legacy domain-service contract sketch for a possible future split. Compliance APIs currently run through services/api and services/ai.
x-contract-status: legacy-domain-sketch
paths:
  /health:
    get:
      responses:
        "200":
          description: Healthy
  /internal/compliance/gaps:
    get:
      summary: List compliance gaps
      responses:
        "200":
          description: Compliance gaps
  /internal/compliance/check:
    post:
      summary: Run compliance check
      responses:
        "202":
          description: Compliance check accepted
