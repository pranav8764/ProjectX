# Observability

Configuration for logs, metrics, and tracing lives here.

Recommended MVP:

- Structured JSON logs from every service
- Correlation ID propagated through gateway, service calls, and events
- OpenTelemetry traces for HTTP requests and document processing jobs
- Metrics for query latency, OCR failures, embedding failures, and retrieval quality

